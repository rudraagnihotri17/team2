<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Live Examination | RedPositive Services Pvt. Ltd.</title>
    <link rel="shortcut icon" type="image/PNG" href="../images/favicon.png">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Jost&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheet/styles.css">
</head>
<body>
    <div class="stream">
            <!-- If teacher is logged in -->
            <% if(isAdmin) { %>
                <div class="imgformbox">
                    <form action="/classroom/uploadassignment" method="POST" enctype="multipart/form-data">
                        <div class="form-group">
                            <label>Select an image of assignment</label>
                            <input type="file" class="form-control" name="uploadImage">
                        </div>
                        <div class="form-group">
                            <button class="button button-submit btn-block">Upload</button>
                        </div>
                    </form>
                </div>

            <!-- Else -->
            <% } else { %>
            <div class="imgformbox">
                <form action="/classroom/uploadsolution" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label>Select your assignment solution(Doc, Pdf only)</label>
                        <input type="file" class="form-control" name="uploadFile">
                    </div>
                    <div class="form-group">
                        <button class="button button-submit btn-block">Upload</button>
                    </div>
                </form>
            </div>
            <% } %>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="../../js/dynamic.js"></script>
</body>
</html>